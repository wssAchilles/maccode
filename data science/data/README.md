# 数据目录说明

## 📁 目录结构

```
data/
├── raw/              # 原始数据 (只读)
│   ├── datasets/     # 数据集文件
│   └── samples/      # 样本数据
│
├── processed/        # 处理后的数据
│   ├── cleaned/      # 清洗后的数据
│   ├── featured/     # 特征工程后的数据
│   └── splits/       # 训练/测试集分割
│
├── models/           # 训练好的模型文件
│   ├── saved/        # 保存的模型
│   ├── checkpoints/  # 训练检查点
│   └── exports/      # 导出的模型
│
└── results/          # 实验结果
    ├── metrics/      # 评估指标
    ├── predictions/  # 预测结果
    └── visualizations/ # 可视化结果
```

## 📝 使用规范

### 1. 原始数据 (raw/)
- **不要修改**原始数据文件
- 数据文件命名格式：`{dataset_name}_{date}.{ext}`
- 示例：`sales_data_20250117.csv`

### 2. 处理数据 (processed/)
- 所有数据处理脚本应该读取 `raw/` 写入 `processed/`
- 保留处理步骤的记录
- 命名格式：`{dataset_name}_v{version}_{processing_step}.{ext}`
- 示例：`sales_data_v1_cleaned.csv`

### 3. 模型文件 (models/)
- 保存模型时包含元数据文件（JSON）
- 命名格式：`{model_type}_{dataset}_{timestamp}`
- 示例：`random_forest_sales_20250117_143022.pkl`

### 4. 结果文件 (results/)
- 保存实验配置和结果
- 使用实验ID或时间戳组织
- 包含 README 或 metadata 文件说明

## ⚠️ 注意事项

1. **数据不会被提交到 Git**
   - 所有数据文件在 `.gitignore` 中被忽略
   - 只有 `.gitkeep` 和 `README.md` 会被提交

2. **生产环境使用 Cloud Storage**
   - 本地 `data/` 目录仅用于开发和测试
   - 生产数据应存储在 Google Cloud Storage

3. **数据安全**
   - 不要在数据文件中包含敏感信息
   - 如需处理敏感数据，先进行脱敏处理

## 🔄 数据同步

从 Cloud Storage 下载数据到本地：
```bash
# 使用脚本（推荐）
python scripts/sync_data.py --download

# 或使用 gsutil
gsutil -m cp -r gs://your-bucket/data/raw/* ./data/raw/
```

上传数据到 Cloud Storage：
```bash
# 使用脚本（推荐）
python scripts/sync_data.py --upload

# 或使用 gsutil
gsutil -m cp -r ./data/processed/* gs://your-bucket/data/processed/
```
